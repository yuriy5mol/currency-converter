# 💱 Currency Converter

Консольный конвертер валют с использованием API [open.er-api.com](https://open.er-api.com).

## 📁 Структура проекта

```
currency_converter/
├── cli.py            # CLI интерфейс
├── api_client.py     # Логика работы с API
├── storage.py        # I/O операции (чтение/запись файлов)
├── http_client.py    # HTTP-клиент для запросов
├── currency_rates.json  # Кэш курсов валют
├── requirements.txt  # Зависимости
└── README.md
```

## 🚀 Установка

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация (Windows PowerShell)
.\.venv\Scripts\Activate.ps1

# Активация (Linux/Mac)
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

## 📖 Использование

### Запуск CLI

```bash
python cli.py
```

**Возможности меню:**
| Пункт | Функция |
|-------|---------|
| 1 | Конвертировать сумму (например, 100 USD → RUB) |
| 2 | Показать список доступных валют (166 шт.) |
| 3 | Обновить курсы валют с API |
| 4 | Показать курс валютной пары |

### Использование как модуль

```python
import api_client

# Конвертация валюты
result = api_client.convert_currency(100, "USD", "RUB")
print(f"100 USD = {result:.4f} RUB")

# Получить список валют
currencies = api_client.get_available_currencies()

# Обновить курсы
api_client.update_currency_rates()
```

## 🔄 Автообновление

Курсы автоматически обновляются если файл `currency_rates.json` старше **24 часов**.

## 📡 API

Используется бесплатный API: `https://open.er-api.com/v6/latest/{base}`

**Ключевые поля ответа:**
| Поле | Описание |
|------|----------|
| `result` | Статус запроса ("success") |
| `base_code` | Базовая валюта (USD, EUR и т.д.) |
| `time_last_update_utc` | Время последнего обновления |
| `time_next_update_utc` | Время следующего обновления |
| `rates` | Словарь курсов (rates.RUB, rates.EUR и т.д.) |

## 📋 Базовые валюты

По умолчанию загружаются курсы для: **USD**, **EUR**, **GBP**, **RUB**

## ⚙️ Конфигурация

В `api_client.py`:
```python
MAX_AGE_HOURS = 24                  # Время жизни кэша (часы)
FAVORITE_CURRENCIES = ["USD", "EUR", "GBP", "RUB"]  # Базовые валюты
```

В `storage.py`:
```python
RATES_FILE = "currency_rates.json"  # Путь к файлу кэша
```
